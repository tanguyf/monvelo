<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/detail_layout"
    android:layout_width="fill_parent"
    android:layout_height="@dimen/detail_height"
    android:layout_gravity="bottom"
    android:background="@drawable/bar_drawable">

    <ImageView
        android:id="@+id/bike"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:adjustViewBounds="true"
        android:paddingBottom="@dimen/image_padding_top_bottom"
        android:paddingLeft="4dp"
        android:paddingTop="@dimen/image_padding_top_bottom"
        android:src="@drawable/ic_bike" />

    <TextView
        android:id="@+id/bike_number"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_toRightOf="@id/bike"
        android:textSize="30sp" />

    <ImageView
        android:id="@+id/parking"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:adjustViewBounds="true"
        android:paddingBottom="@dimen/image_padding_top_bottom"
        android:paddingRight="8dp"
        android:paddingTop="@dimen/image_padding_top_bottom"
        android:src="@drawable/ic_parking" />

    <TextView
        android:id="@+id/parking_number"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_toLeftOf="@id/parking"
        android:paddingRight="8dp"
        android:textSize="30sp" />

    <TextView
        android:id="@+id/station_info"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_toLeftOf="@id/parking_number"
        android:layout_toRightOf="@id/bike_number"
        android:gravity="center"
        android:maxLines="2"
        android:paddingLeft="10dp"
        android:paddingRight="10dp"
        android:textColor="#4a4a4a"
        android:textSize="20sp"
        android:textStyle="italic"
        android:typeface="sans" />

</RelativeLayout>